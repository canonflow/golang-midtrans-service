name: Deploying Go
on:
  push:
    branches:
      - main
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Set up Go
        uses: actions/setup-go@v3
        with:
          go-version: "1.23.3" # Set this to your Go version
      - name: Build Go binary for CentOS 8 (GLIBC 2.28)
        uses: addnab/docker-run-action@v3
        with:
          image: centos:8
          options: -v ${{ github.workspace }}:/app
          run: |
            yum install -y epel-release
            yum install -y golang
            cd /app
            go build -o golang-midtrans-service
      - name: Stop service
        uses: appleboy/ssh-action@v0.1.5
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          passphrase: ${{ secrets.SSH_PASSPHRASE }}
          port: 45022
          script: |
            cd ~/repositories/golang-midtrans-service
            ./stop.sh
      - name: Copy Binary File to Server
        uses: appleboy/scp-action@v0.1.2
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          passphrase: ${{ secrets.SSH_PASSPHRASE }}
          port: 45022
          source: "golang-midtrans-service"
          target: "~/repositories/golang-midtrans-service"
      - name: Restart service
        uses: appleboy/ssh-action@v0.1.5
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          passphrase: ${{ secrets.SSH_PASSPHRASE }}
          port: 45022
          script: |
            cd ~/repositories/golang-midtrans-service
            chmod +x golang-midtrans-service
            ./start.sh
